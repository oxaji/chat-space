.side-bar
  .side-bar__header
    .side-bar__header__name
      .header__lists__user-name
        =current_user.name
      .header__lists__icon
        = link_to new_group_path do
          =icon('far','edit',class: 'icon')
        = link_to edit_user_path(current_user) do
          =icon('fas','cog',class: 'icon')
  .side-bar__groups
    - current_user.groups.each do |group|
      .group
        = link_to '#' do
          .group__name
            = group.name
      .group__message
        メッセージはまだありません
    -# .group
    -#   .group__name
    -#     グループ名
    -#   .group__message
    -#     メッセージ
    -# .group
    -#   .group__name
    -#     グループ名
    -#   .group__message
    -#     メッセージ